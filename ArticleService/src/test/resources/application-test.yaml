spring:
  datasource:
    url: jdbc:h2:mem:test-article
    driver-class-name: org.h2.Driver
  jpa:
    #database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
